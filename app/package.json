{
  "name": "my-app",
  "version": "1.0.0",
  "description": "Sample Node.js app with unit test",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest --ci --coverage --reporters=default --reporters=jest-junit",
    "test:ci": "jest --ci --coverage --reporters=default --reporters=jest-junit"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "supertest": "^6.3.3",
    "jest-junit": "^15.0.0"
  }
}
